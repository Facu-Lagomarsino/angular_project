 // let info = require('./model'); // Importo el módulo index.js por defecto, ya que no se especifica el archivo

 let info = require('./model/model.js'); // Importo el módulo model.js

 console.log(info); // Muestra el objeto exports del módulo model.js